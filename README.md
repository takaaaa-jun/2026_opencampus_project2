# Open Campus Project 2

## フロントエンド開発環境

`frontend/` 配下で Vite + React の開発環境を起動します。

```bash
cd frontend
npm install
npm run dev
```

起動後、表示されたローカルURLをブラウザで開きます。

必要なら、動作確認用にビルドも実行できます。

```bash
npm run build
```
